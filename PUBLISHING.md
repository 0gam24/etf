# 매일 발행 사양 (Daily Publishing Spec) v1.1

시행: 2026-08-13 발행분부터(v1.0) · 본문 구조 개정 2026-08-31부터(v1.1) · 대상: `src/lib/guides.ts`에 추가하는 가이드

이 문서는 **매일 새벽 가이드를 발행하는 루틴이 따라야 할 기준**이다. 발행이 어느 경로로 실행되든(앱 세션 예약·수동·GitHub Actions) 이 기준은 동일하게 적용된다. 기계로 검사 가능한 항목은 [scripts/validate-guide-structure.mjs](scripts/validate-guide-structure.mjs)가 빌드에서 막는다.

기존 발행분에는 소급하지 않는다. 검증기의 `SPEC_RULE_FROM = '2026-08-12'` 이후 발행분만 대상이다. v1.1(본문 골격 개정)도 마찬가지로 시행일 이후 신규 발행분부터만 적용하고 기존 글은 건드리지 않는다.

**v1.1 변경 배경 (2026-08-31)**: 운영자가 검색 콘텐츠 품질 기준(정보 이득·독창적 가치·정보 밀도·중복 제거)을 반영해 본문 구조를 다시 짜라고 지시했다. 주제 선정(§1, 지식iN 질문 기반)과 발행 후 처리(§5)는 그대로 두고, 제목·메타(§2)와 본문 골격(§3)만 개정한다. 이 사이트는 개별 종목 "관련주" 추천형 콘텐츠를 다루지 않으므로(§1의 "개별 종목 매수·매도 권유 금지"), 그런 콘텐츠에만 해당하는 기업 재무 하드데이터 분석(수주잔고·CB/BW 등) 항목은 가져오지 않는다. 스키마·기술 SEO는 페이지 템플릿이 이미 자동 처리하므로(§4) 별도 규정을 추가하지 않는다.

---

## 1. 주제 선정

주제는 **네이버 지식iN 실제 질문**에서만 뽑는다. 사이트 내부 데이터(시세·거래량)는 본문 근거로만 쓰고 주제 선정에는 쓰지 않는다.

```bash
npm run keywords:kin        # 질문 수집 (data/keywords/kin/kin-YYYYMMDD.json)
npm run topic:kin           # 수집분에서 오늘 쓸 후보 추출 (수요 순 · 요일별 렌즈)
```

**발행량은 하루 1편** (2026-08-31 운영자 지시). 새벽 루틴이 매일 KST 06:00에 정확히 1편만 낸다.
아래 슬롯 표는 그 1편을 어느 각도에서 고를지 정하는 기준으로 쓴다. `npm run topic:kin`의
렌즈가 요일에 따라 이 슬롯을 자동으로 돌린다.

| 슬롯 | 기준 | 렌즈 | 요일 |
|---|---|---|---|
| 계절 선행 | 지금 쓰면 1~4개월 뒤 검색이 몰리는 주제 (ISA는 12~1월, 연금저축 세액공제는 5월, 건보료는 11월) | `seasonal` | 월·목 |
| 롱테일 확장 | 기존 글에서 조건·대상을 좁혀 파생시킨 주제 | `longtail` | 수·토 |
| 상시 최대 수요 | 계절과 무관하게 질문이 가장 많이 반복되는 주제 | `evergreen` | 화·금·일 |

이미 노출은 되는데 8~40위에 걸린 쿼리를 보강하는 슬롯은 GSC 실측이 있어야 판단할 수 있어
클라우드 루틴에서는 돌리지 않는다. 로컬에서 `npm run keywords:gsc`를 본 뒤 수동으로 낸다.

**우선순위 신호**

- 같은 취지의 질문이 반복될수록 높게 잡는다. 씨앗 키워드별 수집 건수가 아니라 **질문 내용 기준**으로 센다(씨앗별 건수는 수집 순서가 만든 착시다).
- 3어절 이상 + 조건 수식어(계좌명·연령·금액·시기)가 붙은 질문을 우선한다. 헤드텀은 신규 도메인이 잡기 어렵다.

**통과해야 하는 조건 4가지** (하나라도 안 되면 다음 후보로)

1. 1차 출처(국세청·금감원·건보공단·국민연금공단·KRX·운용사 공시)로 답할 수 있는가
2. 확정수익 보장·투기 조장 없이 쓸 수 있는가
3. 이미 쓴 질문이 아닌가
4. ETF·주식·코인·금융 4개 분야 안인가

**중복 판정**: 제목·본문이 사실상 같은 글만 막는다. 같은 주제라도 조건·대상·각도가 다르면 별도 글로 낸다.

**각도**: 목록 나열보다 판단·이유·비교형이 낫다. 실제로 클릭이 난 세 건이 모두 이 형태였다.
`S&P500 몰빵 괜찮을까`(판단) · `커버드콜 ETF 원금이 녹는 이유`(이유) · `환헤지 vs 환노출`(비교)

---

## 2. 제목·메타

| 항목 | 기준 | 검증 |
|---|---|---|
| `title` | 60자 이내. `keywords[0]`의 어절을 앞 12자 안에 다 넣는다 | ERROR / WARN |
| | 정확일치는 강제하지 않는다. 조사·`ETF`·`vs` 삽입은 자연스러우면 괜찮다 | |
| | `완전정리`·`완전 가이드`·`총정리`·`한눈에 정리` 금지. 그 자리에 조건·대상을 넣는다 | ERROR |
| | 과장·확정 단정어 금지: `무조건`·`100%`·`반드시 오른다`·`대박`·`이것만 보면 끝`·`무조건 수익` 등. 본문에 없는 것을 제목에서 약속하지 않는다 | 자체 점검 |
| | 가운뎃점이 대표 키워드구를 자르지 않게 한다 (`CD금리·파킹형 ETF`는 `CD금리 ETF`를 끊는다) | |
| `description` | 120~155자. ①결론 한 문장 ②검증 가능한 수치·조건 ③이 페이지에서 확인할 수 있는 범위. 키워드를 반복해 채우지 않는다 | ERROR |
| `keywords` | 5~8개. 페이지가 실제로 다루는 것만 | ERROR(3 미만) |
| 공통 | 긴 줄표(`—`) 금지. 상투 문구는 글마다 바꿔 쓴다 | ERROR |

의도어로 자주 검색되는 말(실측): 배당 · 구성종목 · 종목코드 · 분배금 · 차이 · 결제일 · 방법 · 주가

---

## 3. 본문 골격

정보 우선순위는 이 순서를 따른다: **검색의도 → 사실 정확성 → 1차 출처 → 정보 이득(경쟁 문서에 없는 것) → 독창적 해석 → 예외·마찰 지점 → 가독성**. 글자 수를 채우려고 일반론을 반복하지 않는다. 반대로 정보가 부족하다고 핵심 질문을 다 못 푸는 짧은 글도 만들지 않는다. 분량은 "검색의도 해결에 필요한 정보량"으로 정한다.

```
h1  제목
    직답(answer)          결론 1~2문장, 120~150자. 수치는 본문으로
    핵심 포인트(keyPoints) 3~4개, 각각 수치·조건 포함, 45~90자
    비교표(comparisonTable) 마지막 열은 "어느 경우에 유리한가" 또는 "확인할 점"
    목차                   섹션 3개 이상이면 자동 렌더
h2  섹션 5~7개
      첫 문장은 결론형. 질문을 되풀이하지 않는다
      근거(수치·조건·기준일). 기관명·문서명·기준일을 구체적으로 쓴다 ("공식자료에 따르면" 같은 뭉뚱그린 표현 대신)
      예외 또는 다음 행동 한 줄
h2  참고 자료·출처(sources) 1차 출처만
h2  자주 묻는 질문(faq)    4개 이상, 앞 2개는 펼쳐서 렌더
```

**사실과 해석 구분**

- 본문 문장은 사실(1차 출처로 확인된 것)과 해석(그 사실에 대한 판단)이 섞여 무엇이 근거이고 무엇이 저자의 판단인지 알 수 없게 쓰지 않는다. "~입니다"(확인된 사실)와 "~로 볼 수 있습니다/~이 유리합니다"(해석)를 문장 단위로 구분한다.
- 확인 못 한 수치·조항은 쓰지 않는다(§1과 동일). 불확실하면 "관할 기관 안내에서 확인"으로 서술하고 링크만 건다. 이 문장을 정보 제공의 대체물로 남발하지 않는다. 가능한 범위에서 구체적인 확인 방법을 함께 준다.

**정보 밀도(문단 삭제 테스트)**

- 각 문단은 새 사실, 새 데이터, 해석, 비교, 예외, 판단 기준, 후속 질문 해결 중 하나 이상의 역할을 가져야 한다. "이 문단을 지우면 독자가 새 정보를 잃는가?"에 아니오면 그 문단은 쓰지 않는다.
- 같은 결론·같은 수치·같은 설명을 본문과 keyPoints와 FAQ와 결론에서 반복하지 않는다. 특히 "확인할 것"·"체크포인트"·"최종 점검" 같은 종합 정리 블록은 **가이드 하나에 최대 1개**만 둔다.
- 화살표(`→`)로 단계를 나열하는 표현은 글 전체에서 최대 1회. 과정과 인과관계는 대부분 자연스러운 문장으로 쓴다.

**섹션 제목 규칙**

- 무엇에 관한 문단인지 제목만 봐도 알게 쓴다. `얼마나 겹치나`가 아니라 `S&P500과 나스닥100은 상위 종목이 얼마나 겹치나`
- 의문형 제목은 1~2개까지. 전부 의문형이면 상투적으로 읽힌다
- `howTo: true`인 가이드는 제목이 단계명으로 쓰이므로 의문형을 쓰지 않는다
- 긴 줄표 금지

**결론(마지막 섹션)과 FAQ**

- 마지막 섹션은 앞선 섹션의 수치·조건을 다시 나열하는 요약이 아니라, 독자가 다음에 무엇을 확인하거나 판단해야 하는지로 마무리한다("어떤 상황이면 어느 쪽인지", "무엇을 관할 기관에서 재확인할지").
- FAQ는 본문에서 이미 완전히 답한 질문을 그대로 복사하지 않는다. 예외·경계 조건·흔한 오해처럼 본문에서 다루기엔 작은 후속 질문만 넣는다. 새 정보가 없는 항목은 아예 뺀다.

**필수 필드**

| 필드 | 기준 |
|---|---|
| `answer` | 필수. 가이드마다 고유 문장 |
| `keyPoints` | 필수. 본문 문장 재진술 금지. 본문에 없는 판단·조건·비교 결과만 |
| `sources` | 필수. 주제 관할 기관 |
| `sourceQuestions` | 필수. 지식iN 원문 주소(`docId` 포함) |
| `comparisonTable` | 비교·판단형 주제면 권장. 5행 4열 이상 |
| 리스크·반대 관점 섹션 | 권장. 투자 주제라 한쪽만 쓰면 신뢰가 깎인다 |

**클러스터 배정**: 새 슬러그는 `GUIDE_CLUSTERS` 중 하나에 반드시 넣는다. 배정하지 않으면 관련 가이드가 주제와 무관하게 붙고, 그 글은 아무도 링크하지 않는 상태가 된다.

---

## 4. 스키마

가이드 페이지가 자동으로 붙인다. 별도 작업 없음.

- `Article` (저자는 Organization, 이미지는 페이지 og:image와 동일)
- `BreadcrumbList` · `FAQPage`
- `citation` (sources를 기관명과 함께)
- `speakable` (직답 블록이 렌더될 때만)
- `HowTo` (`howTo: true`일 때)

---

## 5. 발행 후 처리

| 시점 | 할 일 |
|---|---|
| T+0 | 발행 커밋 push |
| 배포 완료까지 | 자동 대기. `npm run push:guides`가 모든 주소가 열릴 때까지 확인한 뒤에 통보한다 |
| 배포 확인 직후 | `npm run push:guides` (IndexNow → Bing·네이버, 사이트맵 캐시 갱신) |
| 확인 | 새 주소 200 응답, canonical, og:image |

색인 통보를 배포보다 먼저 보내면 봇이 404를 받고 주소를 버린다. 그래서 통보 전에 대기하도록 되어 있다. 직접 실행할 때도 배포 완료 후에 돌린다.

**클라우드 루틴에서 돌릴 때**는 `SITE_URL`을 함께 준다. `.env.local`이 없는 컨테이너에서는
`SITE_URL`이 비어 색인 단계가 통째로 건너뛰어지고, 배포 확인까지 같이 사라진다.

```bash
SITE_URL=https://iknowhowinfo.com npm run push:guides
```

`INDEXNOW_KEY`·`GOOGLE_INDEXING_KEY`는 로컬 전용이라 컨테이너에서는 두 채널이 자동으로
건너뛰어진다. 배포 확인(새 주소가 200을 낼 때까지 대기)은 키 없이도 그대로 돌아가므로,
루틴에서는 이 명령을 "글이 실제로 사이트에 떴는지 확인하는 단계"로 쓴다.
검색엔진 통보까지 마치려면 배포 후 로컬에서 `npm run push:guides`를 한 번 더 돌린다.

---

## 6. 발행 전 자체 점검

```bash
npm run validate:guides    # 구조 검증 (빌드에도 연결됨)
npm run cf:build           # 빌드 성공 확인
```

검증기가 ERROR를 내면 빌드가 막힌다. WARN은 막지 않지만 새 글에서는 없애는 것을 기본으로 한다.

---

## 관련 문서

- [CLAUDE.md](CLAUDE.md) 보안·UX 카피·문체·주제 선정·push 정책
- [SEO.md](SEO.md) 사이트 전체 SEO 규칙
